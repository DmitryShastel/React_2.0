import React from 'react';

export const OnOff = () => {
    return (
        <div>
            <button onClick={() => {}}>on</button>
            <button onClick={() => {}}>off</button>
            <input type='color'/>
        </div>
    );
};

